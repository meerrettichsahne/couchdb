server.port = 8082

debug=false
logging.level.root=INFO
spring.codec.max-in-memory-size=3MB

# Database
spring.datasource.url=jdbc:postgresql://hostname:5432/schema
spring.datasource.hikari.schema=public
spring.datasource.username=set using runtime parameter
spring.datasource.password=set using runtime parameter
spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.open-in-view=true

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= false
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update